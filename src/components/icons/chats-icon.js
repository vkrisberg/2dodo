import React, {PureComponent} from 'react';
import {Svg, G, Path} from 'react-native-svg';

import CircleIcon from './circle-icon';

export default class ChatsIcon extends PureComponent {
  render() {
    return (
      <CircleIcon>
        
      </CircleIcon>
    );
  }
}