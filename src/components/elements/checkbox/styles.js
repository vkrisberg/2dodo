import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  commonWrapperStyles: {
    borderWidth: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },

  label: {
    color: 'white',
    marginRight: 10
  }
});