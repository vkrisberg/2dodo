import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  arrow: {
    width: 21,
    height: 21,
    padding: 10
    
  },

  arrowLeft: {
    // transform: [{ rotate: '-45deg'}]
  },
  
  wrapper: {
    width: 300,
    height: 60,
    left: 15,
    position: 'absolute',
    marginTop: 15
  }
});