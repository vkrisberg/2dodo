import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    width: 300,
    height: 45,
    borderColor: '#dcdcdc',
    borderWidth: 1,
    marginBottom: 15,
    color: '#aeadb0',
    padding: 15,
    borderRadius: 4
  },

  inputFocused: {
    borderWidth: 3,
    borderColor: '#e6e6e6'
  }
});
